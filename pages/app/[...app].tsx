import { LayoutApp } from '@layouts/layoutApp';
import { Fragment } from 'react';

const CatchAllApp = () => {
  return (
    <LayoutApp>
      <Fragment />
    </LayoutApp>
  );
};
export default CatchAllApp;
